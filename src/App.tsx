import type { FC } from 'react'

const App: FC = () => (
  <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
    <h1>公共组件库 — 开发环境</h1>
    <p>组件库已成功挂载，可在此页面调试和预览组件。</p>
  </div>
)

export default App
